import { h } from 'preact';
import { Link } from 'preact-router/match';
import style from './style.css';

const Ad = () => (
	<div class={style.ad}>
		The advertisement goes here
	</div>
);

export default Ad;
